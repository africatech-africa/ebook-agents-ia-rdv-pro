// src/index.ts — point d'entrée du projet RDV-Pro.
//
// Ce fichier est volontairement (presque) vide : c'est À TOI de le
// remplir, chapitre après chapitre, en suivant l'ebook. Le but de ce
// starter est que tu écrives le code toi-même — pas que tu le lises.
//
// Lance-le avec :  npm run dev   (ou : npm start)

// Le `: string` ci-dessous, c'est du TypeScript : il dit que `next`
// contient forcément du texte. C'est le premier concept que le
// chapitre 1 t'expliquera. Si la commande tourne, ta chaîne
// tsx + tsconfig est prête.
const next: string = "Chapitre 1 — Pourquoi les agents IA";

console.log("RDV-Pro — starter prêt. À toi de jouer !");
console.log(`Prochaine étape : ${next}`);
